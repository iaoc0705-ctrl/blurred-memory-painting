# 朦胧记忆油画 · 使用说明书

调用名 `$blurred-memory-painting`　｜　版本 0.3.0　｜　2026-09-17

---

## 一、它是什么

一个把画面变成「说不清是谁、却像见过」的失焦写意油画的工具。它同时接两种输入：**一段文字**，或**你的一张照片**。

它解决的核心问题是：为什么有些画让人想起自己的记忆。答案是——**抽走会暴露身份的信息，同时留下一个足够强的情绪钩子**。面孔、文字和具体身份线索一律不给；姿势、距离、光、天气、物件残片和偶然的暖色可以留下。于是画面作为「情境」成立，作为「记录」无法被完整考证，观众就会自动把自己的经历填进去。

因此它**不是**：照片加虚焦滤镜、镜头焦外虚化、写实肖像、奇幻或恐怖氛围、任何在世画家具体作品的变体。

---

## 二、六种用法（复制即用）

| 你想做什么 | 这样说 | 你会拿到 |
| --- | --- | --- |
| 凭空生成一张 | `用 $blurred-memory-painting 生成：黄昏的海边，一个人背对着站在防波堤上，低角度落日在他背后` | 一张成图 + 一句场景说明 |
| 改画自己的照片 | 发照片，加一句 `把这张照片画成这种风格` | 一张成图；姿势与服装守住，脸化掉 |
| 先看方案再决定 | `先给我三个方案，我挑一个再画` | 三段文字概念，等你点头 |
| 出一组图集 | `做一组 12 张，海边和车站都要有` | 12 张同调性的画，逐张换地点与天气 |
| 只要提示词 | `只要提示词，不要生成` | 中英提示词各一段，可贴去别的模型 |
| 只要分析 | `分析这张画为什么有记忆感` | 文字分析，按视觉语法逐项拆 |

**触发方式**：会自动触发（你说"画一张面孔看不清的油画"这类话它就会接手），也可以用 `$blurred-memory-painting` 点名调用。

---

## 三、从输入到输出的完整流程

| 步骤 | 发生什么 | 你看得到吗 |
| --- | --- | --- |
| 1 触发 | 名称与描述常驻上下文，命中才加载正文 | 看不到 |
| 2 分流 | 判断你给的是：文字 / 自己的照片 / 别人的画 / 只要文字 | 看不到 |
| 3 选模式 | 单张（默认）｜导演｜系列，按你的措辞自己判断 | 只有导演模式会停下来问你 |
| 4 构思 | 从八行概念引擎各取一项，组三个场景，按五维打分选一个 | 看不到（导演模式除外） |
| 5 拼提示词 | 读概念引擎、调色板、笔触库，写出正面写法 + 禁区清单 | 你要求时才给 |
| 6 生成 | 内置图像工具；不可用时走 `scripts/stylize_photo.py` | 一张图 |
| 7 自检 | 对七条清单逐条检查 | 看不到 |
| 8 修正 | 只修硬伤；不因为落日、暖色、地点暧昧、道具不清或空场比例而抹掉第一版的生命力 | 最多一次 |
| 9 交付 | 成品复制到工作区，附一句不带解释的场景说明 | 图 + 说明 |

两个刻意的设计：**构思三选一不给你看**（避免越生成越差），**最多只修一次**（不生成备选、不无限迭代）。但修正前先问：这是硬伤，还是只是审美偏好？如果第一版有情绪钩子，默认保留第一版。想要备选就说"先给我三个方案"。

---

## 四、改画照片：什么守住、什么改掉

| 守住（让本人认得出） | 改掉（让它成为记忆） |
| --- | --- |
| 人物数量、姿势、重心、动作方向 | 媒介：一律重画成布面油画，干笔、擦涂、边缘化开、布纹可见 |
| 两人之间的间距与朝向 | 面孔：能看清的脸、五官、表情、胡须全部化掉 |
| 构图骨架、人物在画面中的比例 | 光线：重做，默认逆光，受光面小于两成 |
| 服装色块、原照片自带的叙事性暖色 | 环境：删掉招牌、箭头、品牌、屏幕、手机、镜子、天花、人群 |
| 关键道具的存在（行李箱、伞、狗） | 空间：可整体替换，机场可以变成一道光的门口 |
| 原照片的长宽比 | 色彩：压到低饱和，但保留有叙事作用的暖色 |

**判定标准**：本人认得出"这是我、这是当时的姿势和关系"，陌生人看到的是一张不知道是谁的画。两个条件同时成立才算成功。

**一句必须提前知道的话**：认不出你的脸是设计，不是失败。负责"这是谁"的是姿势、距离和服装色块，不是五官。所以正脸大头照会退化成侧影或背影。

---

## 五、输出规格

- **画幅**　文生图默认略偏竖（3:4 或 4:5），让天空与水面在人物上下展开；两人并肩或需要大范围地平线时才用横幅。改画照片一律沿用原图长宽比。
- **尺寸**　常用 `1024x1280`（4:5）、`1024x1536`（2:3）、`1536x1024`（3:2）。硬约束：边长是 16 的倍数，长宽比不超过 3:1。
- **落点**　内置工具生成后会把成品复制到工作区或你指定的目录；兜底脚本用 `--output` 直接落盘。
- **命名**　按内容命名，例如 `01-waiting-at-the-water.png`，不用随机串。
- **交付**　默认只给一张图和一句话；不附长篇解释，留白由画面完成。

---

## 六、它会怎么自检

每次生成后逐条过：

1. 面孔是否真的不可辨认，而不是"有点模糊"？
2. 人物边缘是否化进背景，而不是贴着一个清晰轮廓？
3. 这是一张画，还是加了滤镜的照片？
4. 空场是否给记忆留下了位置，还是被无关信息堵死？
5. 暖色是在服务时间感和情绪，还是变成了装饰性风景？
6. 是否完全不出现可读文字、标志、落款？
7. 是否与参考图或任何具体作品明显相似？
8. 如果第一版被修过，修正是否真的针对硬伤，并保住了原来的情绪钩子？

---

## 七、边界

- **不给面孔**　这是风格的定义，不是可以商量的选项。
- **不出现人群**　只画一到两人；三人以上会收敛，其余删掉或推到远景变成形状。
- **不做写实肖像**　要保留清晰五官的请求会撞上反模式，会被拒绝或改成背影。
- **不模仿具体作品**　用户说"某某画家风格"时，名字会被翻译成视觉属性（颜色、笔法、光位、留白、尺度），生成原创画面，而非某张画的变体。
- **不打包第三方画作**　仓库与 skill 内不含任何他人作品；参考图只用于提炼视觉语言。
- **硬反模式**　出现才判失败：镜头虚化／焦外光斑／径向或高斯模糊／移轴、磨皮与空气刷、清晰五官、双曝光幽灵、奇幻恐怖氛围、照片叠棕褐色滤镜、人群与表演性情绪、画面中的任何文字与落款。完整落日、较大暖色区域、地点暧昧、道具不清和空场不足是软风险；只在它们压扁了未完成故事时才修。

---

## 八、文件地图

| 文件 | 作用 | 什么时候被读 |
| --- | --- | --- |
| `SKILL.md` | 核心机制、四个决策、视觉 DNA、反模式、照片改画、输出模式、生成与迭代 | 每次调用 |
| `references/prompt-patterns.md` | 概念引擎表、英文与中文提示词骨架、十条修正句式、系列图集规则 | 构思与拼提示词时 |
| `references/visual-grammar.md` | 调色板、笔触库、人物处理、空场、室内、十条失败诊断 | 定调色笔触或诊断失败时 |
| `references/photo-edit.md` | 保留／改写清单、编辑版骨架、按照片类型应对、编辑专属修正句式 | 改画照片时 |
| `scripts/generate_image.py` | 文生图兜底，走 `/v1/images/generations`，读 `O10_API_KEY` | 内置图像工具不可用时 |
| `scripts/stylize_photo.py` | 图生图兜底，走 `/v1/images/edits`，读 `O10_API_KEY` | 内置图像工具不可用时 |
| `examples/` | 三张实测示例、提示词原文、诊断说明 | 想对照效果时 |
| `README.md` | 面向人的简介与安装 | 发布与分享 |

---

## 九、故障排除

| 症状 | 你该说 |
| --- | --- |
| 出来的像加了滤镜的照片 | 太像照片了，把镜头模糊换成干笔拖扫和布纹 |
| 脸还是能认出来 | 脸再化掉，把头转过去或压进阴影里 |
| 认不出照片里是我 | 恢复原来的姿势、行进方向和外套颜色 |
| 姿势被"修正"了 | 保持原来的站姿和行进方向，不要修正姿势 |
| 构图被重排 | 保持原图的长宽比和人物在画面里的位置比例 |
| 暖色太橙太亮 | 暖色压向赭黄和铁锈，面积不要扩大 |
| 背景里的招牌还在 | 逐项删掉招牌、箭头、屏幕、镜子和天花 |
| 变成了明信片式夕阳 | 只有在景色完全盖住人物故事时才削弱太阳或金光；保留能制造时间感的暖色，并增加距离、遮挡或未完成动作 |
| 人群删不掉 | 只保留一到两个人，其余变成远景里的形状 |
| 提示没有可用的图像工具 | 走 `scripts/stylize_photo.py`，需要 `O10_API_KEY` |

---

## 十、版本与安装

**当前版本 `v0.3.0`**：在 v0.2.0 的文生图、照片改画、参考图视觉语言提炼、单张／导演／系列三种模式与图生图兜底脚本之上，修正了硬约束与软倾向的边界，加入“只修硬伤、不修偶然性”的迭代逻辑，并支持以情绪钩子为第一评分项的系列创作。

**安装**：把整个目录放进 Codex skills 目录即可，运行期只需 `SKILL.md`、`agents/`、`references/`、`scripts/` 四部分。

```text
skills/
└── blurred-memory-painting/
    ├── SKILL.md
    ├── agents/openai.yaml
    ├── references/
    └── scripts/
```
